listening-port=3478
listening-ip=0.0.0.0
relay-ip=0.0.0.0

# Replace with your real public IP address before starting coturn.
external-ip=24.200.106.149

realm=videoconf.local

# Long-term credentials used by the WebRTC clients.
lt-cred-mech
user=videoconf:videoconf123!

# Keep the server focused on TURN/STUN over UDP/TCP for WebRTC.
fingerprint
no-cli
no-tls
no-dtls

log-file=/var/lib/coturn/turnserver.log
